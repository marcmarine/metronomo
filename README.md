# Metrónomo Online

Web metronome without UI dependencies: SVG + Web Audio API (scheduler with _lookahead_) + TypeScript, bundled with Vite.

## Development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build    # checks types and generates dist/
npm run preview  # serves dist/ locally to check it
```
